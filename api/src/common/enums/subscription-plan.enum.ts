export enum SubscriptionPlan {
  FREE = 'free',
  PRO = 'pro',
  ELITE = 'elite',
  ENTERPRISE = 'enterprise',
}

export enum SubscriptionStatus {
  ACTIVE = 'active',
  CANCELLED = 'cancelled',
  EXPIRED = 'expired',
  TRIALING = 'trialing',
}
